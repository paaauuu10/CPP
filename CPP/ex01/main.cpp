#include <iostream>

int main(void)
{
    std::string    input;
    bool    loop = true;

    std::cout << "Hi! What do you want a do?" << std::endl;
    while (loop)
    {
        std::cout << "ADD, SEARCH or EXIT " << std::endl;
        if 
        loop = false;
    }
    return (0);
}